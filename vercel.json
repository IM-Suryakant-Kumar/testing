{
    "buildCommand": "npm i --prefix client && npm run build --prefix client",
    "outputDirectory": "client/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/server/(.*)",
            "destination": "/server/index.js"
        }
    ]
}